#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks for uppercase character
 * @c: input letter
 * Return: 1 if uppercase, 0 if otherwise
 */

int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
